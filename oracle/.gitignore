# Build artifacts from the trace-format compile-check.
build/
/tfcheck
/tfcheck.exe

# Demo / captured trace streams — regenerate with make_sample_traces.py.
*.trace

# Python bytecode cache.
__pycache__/

# Local Dolphin oracle user dir + build log (regenerable).
dolphin-user/
build_dolphin.log
